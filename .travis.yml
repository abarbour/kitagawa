# .travis.yml for kitagawa

language: r
warnings_are_errors: true
sudo: required

#apt_packages:
# - libgmp3-dev
# - libmpfr 

r_binary_packages:
  - kelvin
  - knitr
  - RColorBrewer
  - signal
  - testthat
  
r_github_packages:
  - jimhester/covr

notifications:
  email:
    on_success: change
    on_failure: change
    
after_success:
  - Rscript -e 'library(covr);codecov()'